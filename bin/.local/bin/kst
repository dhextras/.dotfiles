#!/bin/bash

# Kill SSH tunnel on a given port (default: 5901)
PORT=${1:-5901}
PID=$(lsof -ti tcp:"$PORT")

if [ -n "$PID" ]; then
    echo "Killing SSH tunnel on port $PORT (PID: $PID)..."
    kill -9 "$PID"
    echo "Done."
else
    echo "No SSH tunnel found on port $PORT."
fi
